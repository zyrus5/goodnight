services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: goodnight
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d goodnight"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
